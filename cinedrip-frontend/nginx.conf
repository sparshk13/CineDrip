server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Proxy API to backend. Uses a variable + Docker's embedded DNS resolver so the
  # container still serves the SPA even if the "backend" host is not yet resolvable
  # at nginx startup (avoids "[emerg] host not found in upstream" boot failure).
  location /api/ {
    resolver 127.0.0.11 valid=10s ipv6=off;
    set $upstream_backend http://backend:3000;
    proxy_pass $upstream_backend;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
